import java.util.ArrayList;
import java.util.Arrays;

// Test of a Complete Neuron

public class Complete_Neuron {

    Perceptron A = new Perceptron(0, 0, 5);
    Perceptron B = new Perceptron(0,0, 5);
    Perceptron C = new Perceptron(0, 0, 5);

    ArrayList<Double> Inputs = new ArrayList<Double>();
    ArrayList<Double> Layer_01 = new ArrayList<Double>();
    ArrayList<Double> Layer_02 = new ArrayList<Double>();
    ArrayList<Integer> Output = new ArrayList<>();
    
    ArrayList<Integer> Expected = new ArrayList<>(Arrays.asList(1,0,0,0));

    for(int i = 0; i < Math.pow(2,n); i++){

        

    }









}
 